# Device Targeting for Push Notifications

## 🎯 ฟีเจอร์ใหม่: ส่ง Notification แบบเลือกเครื่อง

ตอนนี้คุณสามารถส่ง push notification ไปยังเครื่องที่เลือกได้แล้ว!

## 🔧 การตั้งค่าเพิ่มเติม

### 1. อัปเดต Database Schema

```bash
# สร้าง migration สำหรับ schema ใหม่
npx prisma db push

# หรือสร้าง migration file
npx prisma migrate dev --name add-device-info
```

### 2. รีสตาร์ท Development Server

```bash
npm run dev
```

## 📱 วิธีการใช้งาน

### 1. **Subscribe Devices**
- เปิดเว็บไซต์ในเบราว์เซอร์หลายเครื่อง
- กดอนุญาต notifications และ subscribe ในแต่ละเครื่อง
- ระบบจะสร้าง device ID และเก็บข้อมูลเครื่องอัตโนมัติ

### 2. **ดูรายการ Devices**
- ในหน้าหลัก จะแสดงรายการเครื่องที่ subscribe แล้ว
- แสดงข้อมูล: ชื่อเครื่อง, platform, เวลาใช้งานล่าสุด

### 3. **เลือกเครื่องเป้าหมาย**
- **Send to All Devices**: ส่งไปทุกเครื่อง
- **Select Specific Devices**: เลือกเครื่องที่ต้องการส่ง

### 4. **ส่ง Notification**
- กรอกข้อความ
- เลือกเครื่องเป้าหมาย
- กด "Send Notification"

## 🔍 ข้อมูลที่เก็บของแต่ละ Device

```typescript
{
  deviceId: "unique-device-identifier",
  deviceName: "Chrome on iPhone", // ชื่อที่แสดงใน UI
  platform: "iOS", // Android, iOS, Windows, macOS, Linux
  userAgent: "Mozilla/5.0...", // ข้อมูล browser
  userId: null, // สำหรับระบบ user authentication (ถ้ามี)
  isActive: true, // เครื่องที่ยังใช้งานอยู่
  lastUsed: "2024-01-01T12:00:00Z", // เวลาใช้งานล่าสุด
  createdAt: "2024-01-01T10:00:00Z" // เวลาที่ subscribe
}
```

## 🚀 API Endpoints ใหม่

### 1. **GET /api/devices**
ดึงรายการเครื่องทั้งหมด
```typescript
// Response
{
  devices: Device[]
}
```

### 2. **POST /api/notify-targeted**
ส่ง notification แบบเลือกเครื่อง
```typescript
// Request
{
  title: string,
  body: string,
  sendToAll?: boolean,
  deviceIds?: string[], // ส่งไปเครื่องที่เลือก
  userIds?: string[]    // ส่งไปทุกเครื่องของ user (ถ้ามี user system)
}

// Response
{
  message: string,
  results: {
    deviceId: string,
    deviceName: string,
    success: boolean,
    error?: string
  }[],
  summary: {
    total: number,
    success: number,
    failed: number
  }
}
```

## 💡 Use Cases

### 1. **Testing**
- ส่ง notification ไปเครื่องทดสอบเท่านั้น
- ไม่รบกวนเครื่อง production

### 2. **Platform-specific Notifications**
- ส่งไปแค่ Android หรือ iOS
- แจ้งเรื่อง platform-specific features

### 3. **User-specific Notifications**
- ส่งไปทุกเครื่องของ user คนหนึ่ง
- Personal notifications

### 4. **Device Management**
- ดูเครื่องไหนยัง active อยู่
- ปิดการใช้งานเครื่องเก่า

## 🔒 การจัดการ Device

### Automatic Cleanup
- เครื่องที่ subscription หมดอายุจะถูกทำเครื่องหมาย `isActive: false`
- ระบบจะไม่ส่ง notification ไปเครื่องที่ไม่ active

### Manual Management
คุณสามารถเพิ่ม API สำหรับ:
- ลบเครื่องออกจากระบบ
- เปลี่ยนชื่อเครื่อง
- ดู usage statistics

## 🎨 การปรับแต่ง UI

### เปลี่ยนชื่อเครื่อง
```javascript
import { setDeviceName } from '@/utils/deviceUtils'

// ให้ user ตั้งชื่อเครื่องเอง
setDeviceName("My iPhone")
```

### เพิ่ม User Authentication
```typescript
// ใน subscribeToNotifications()
const subscriptionData = {
  ...subscription,
  deviceId,
  deviceName,
  userAgent: deviceInfo.userAgent,
  platform: deviceInfo.platform,
  userId: currentUser?.id // เพิ่ม user ID
}
```

## 🐛 Troubleshooting

### Device ไม่แสดงในรายการ
1. ตรวจสอบว่า subscribe สำเร็จ
2. ดู console logs ใน browser
3. ตรวจสอบ database ว่ามีข้อมูลหรือไม่

### Notification ส่งไม่ได้
1. ตรวจสอบ device ยัง active อยู่หรือไม่
2. ดู response จาก API
3. ตรวจสอบ VAPID keys

### Performance
- Database มี index สำหรับ deviceId, userId, isActive
- API จะส่งแบบ parallel ไปทุกเครื่อง
- Inactive devices จะถูกข้ามอัตโนมัติ